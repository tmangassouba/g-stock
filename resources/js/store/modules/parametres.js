// import userApi from '../../api/user'
// import notifier from '../../notifier'
// import handleShowMsg from "../../handleShowMsg";

const state = {
    monaie: 'F CFA',
}

const mutations = {
    //
}

const actions = {
    //
}

const getters = {
    monaie: (state) => {
        return state.monaie
    },
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}